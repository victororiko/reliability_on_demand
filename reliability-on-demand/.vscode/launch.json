{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to https://localhost:5001/",
      "request": "launch",
      "type": "chrome",
      "url": "https://localhost:5001/",
      "webRoot": "${workspaceFolder}"
    }    
  ]
}