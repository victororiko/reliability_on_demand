import React from 'react'

interface Props {
    
}

export const FilterExpressionDetailsList = (props: Props) => (
        <div>
            Filter Expression Details list with checkboxes goes here.
        </div>
    )
