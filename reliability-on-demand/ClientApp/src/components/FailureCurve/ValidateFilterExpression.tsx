import React from 'react'

interface Props {
    
}

export const ValidateFilterExpression = (props: Props) => (
        <div>
            Validate Filter Expression Button goes here
        </div>
    )
